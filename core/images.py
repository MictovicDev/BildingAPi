import requests
import base64

# Encode the image as base64

# def imageconverter():
#     with open("C:/Users/user/bildingapi/bilding/media/images/IMG_4701_c1ZJ44P.jpg", "rb") as image_file:
#       base64_encoded_image = base64.b64encode(image_file.read()).decode("utf-8")
#     payload = {
#         "image": base64_encoded_image,
#         "other_field": "other_value"  # Include other fields as needed
#     } 
#     # response = requests.post('http://localhost:8000/api/requests/', json=payload)
#     return payload
    
    # if response.status_code == 201:
    #     print('Object created successfully.')
    # else:
    #     print('Object creation failed:', response.status_code)
   






# Create a payload with the base64-encoded image
   
# Send a POST request to create the object
# response = requests.post('http://localhost:8000/api/create/', json=payload)

# if response.status_code == 201:
#     print('Object created successfully.')
# else:
#     print('Object creation failed:', response.status_code)
